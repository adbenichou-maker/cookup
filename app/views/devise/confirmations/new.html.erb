<% content_for :hide_navbar, true %>

<div class="auth-container">
  <!-- Header -->
  <div class="auth-header">
    <%= link_to new_user_session_path, class: "auth-back-link" do %>
      <i class="fa-solid fa-arrow-left"></i>
    <% end %>
    <h1 class="auth-brand">CookUp</h1>
  </div>

  <!-- Welcome Section -->
  <div class="auth-welcome">
    <div class="auth-icon blue">
      <i class="fa-solid fa-envelope-circle-check"></i>
    </div>
    <h2 class="auth-title">Confirm Email</h2>
    <p class="auth-subtitle">Resend confirmation instructions to your email</p>
  </div>

  <!-- Form Card -->
  <div class="auth-card">
    <%= simple_form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: { method: :post }) do |f| %>
      <%= f.error_notification message: "Please fix the errors below:" %>
      <%= f.full_error :confirmation_token %>

      <div class="auth-form">
        <div class="auth-input-group">
          <label class="auth-label">Email</label>
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      label: false,
                      placeholder: "Enter your email address",
                      value: (resource.pending_reconfirmation? ? resource.unconfirmed_email : resource.email),
                      input_html: { autocomplete: "email", class: "auth-input" } %>
        </div>

        <%= f.button :submit, "Resend Confirmation", class: "auth-submit" do %>
          <span>Resend Confirmation</span>
          <i class="fa-solid fa-paper-plane"></i>
        <% end %>
      </div>
    <% end %>
  </div>

  <!-- Footer Link -->
  <div class="auth-footer">
    <p>Already confirmed? <%= link_to "Sign In", new_user_session_path, class: "auth-link" %></p>
  </div>
</div>
